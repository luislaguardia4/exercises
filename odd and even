public class App {
    public static void main(String[] args) throws Exception {
      
        System.out.println("Even #");
        for (int i = 2; i <= 100; i += 2) {
            System.out.println(i);
        }

//start the loop from 2 (the first even number) 
//and incrment by 2 in each iteration, then all even numbers between 1 and 100.
//The loop continues until the value of i is less than 100 or euqal to 100.
//the one i print (i) statement inside the loop prints each even number on a new line.

        System.out.println("Odd #");
        for (int i = 1; i <= 100; i += 2) {
            System.out.println(i);
        }

//it is all pretty much the same, i just had to change the value of i to 1 to find odd numbers then increment it


    }
}
